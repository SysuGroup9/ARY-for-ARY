# DevCompass Oval Notes

MVP calibration notes:

- Track profile uses explicit polyline centerline points as the runtime source of truth.
- Background SVG is visual only; horse positions are never inferred from pixels.
- Four lanes are offset from the sampled centerline normal.
- Message bubbles avoid the start / finish safe zone.

