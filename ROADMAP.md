# ARY GRS 001 ROADMAP

## 1. 任务背景

目标是基于 ARY 的产品需求文档，实现一个可运行的 GRS 001 PoC，用来证明以下核心命题：

1. Race 数据主权属于 Organizer。
2. ARY 不需要持久化完整 Race 数据。
3. ARY 仍然可以创建、披露、组织、展示赛事。
4. ARY 的展示内容来自 Organizer 主动披露的公开数据。

当前工作区为空项目，需要先完成需求拆解、方案设计、计划确认，再进入代码实现。

## 2. 已读取的需求来源

- 会话中成功读取到的 `PRD(5).md` 内容。
- 本地可稳定访问的 [PRD(4).md](C:/Users/xy/Desktop/PRD(4).md)。

说明：
- 用户提到的是 `PRD(5).md`，但后续终端无法再次稳定解析该路径。
- 目前计划基于已经成功读取到的 `PRD(5).md` 内容以及可访问的 `PRD(4).md` 交叉还原。
- 如果 `PRD(5).md` 与 `PRD(4).md` 有关键差异，后续实现前应以用户重新提供的最终版为准。

## 3. 需求摘要

### 3.1 角色

- Organizer：创建赛事、配置评分、查看榜单、处理反馈、修改题目、赛后评论、清理数据。
- Participant / Rider：报名、提交代码和 Riding Record、查看排名、发送反馈。
- Audience：无需登录，查看公开赛事、榜单和赛后展示。

### 3.2 必须覆盖的能力

- 赛事列表按状态展示：报名中、报名结束、比赛中、比赛结束。
- Organizer 创建赛事：
  - 基础信息
  - 时间配置
  - 评分模型
  - 赛后展示选项
  - 每组人数上限
  - 提交频率限制
- Participant 报名参赛，组员人数受限。
- Participant 在比赛期间提交代码和 Riding Record。
- Organizer Runner 可拉取待评测任务并回传结果。
- 榜单可更新并公开展示。
- Participant 可向 Organizer 发送反馈，Organizer 可回复并标记状态。
- Organizer 可在比赛期间修改题目/训练数据，并通知参赛队伍。
- 比赛结束后按披露选项展示评论、题目、训练数据、亮点、代码等内容。
- Organizer 可一键清除比赛。

### 3.3 数据边界

- Organizer 私有：
  - 测试代码
  - 关键词配置
  - 完整评分记录
  - 未公开的原始 Race 数据
- ARY 持久化：
  - 公开赛事元数据
  - 账号与角色数据
  - 队伍/报名关系
  - 榜单投影
  - 反馈与通知
  - 赛后公开展示投影
- ARY 临时存储：
  - 待 Runner 拉取的代码压缩包
  - 待 Runner 拉取的 Riding Record

## 4. 当前设计判断

### 4.1 推荐实现形态

推荐先做一个单仓的 Web PoC，而不是上来拆多服务。原因：

- 当前没有现成代码库，先把产品闭环跑通更重要。
- PRD 的核心是验证数据边界与交互流程，不是验证微服务复杂度。
- Runner 集成可以通过一组受控 API 路由模拟出来，后续再拆。

### 4.2 推荐技术栈

- 前端/全栈框架：Next.js + TypeScript
- 样式：Tailwind CSS
- 持久化：SQLite
- ORM：Prisma
- 表单校验：Zod
- 临时文件：本地文件系统临时目录

原因：
- 便于快速同时实现页面、API、角色流和 Runner 接口。
- SQLite 足够承载 PoC，且能清晰控制哪些数据允许持久化。
- 文件系统临时目录更适合处理中转 zip/txt 文件，不应把提交文件塞进关系库。

## 5. 已识别的关键矛盾与风险

### 5.1 赛后下载 vs 临时删除

PRD 同时要求：

- 代码与 Riding Record 在评测完成后删除。
- Organizer 赛后还能下载各队伍最高分提交的代码与 Riding Record。

这两个要求天然冲突。

当前建议：
- ARY 只做任务中转和结果展示。
- Runner 在 Organizer 侧保存最高分提交的归档。
- ARY 的“下载”按钮改为请求 Organizer 公开的导出接口，或仅提供导出清单。

如果用户坚持“下载文件由 ARY 直接提供”，则必须放宽“不持久化完整 Race 数据”的约束。

### 5.2 localStorage vs 多角色协作

会话中读到的较长 PRD 提到“数据存储使用浏览器 localStorage”，但可访问的 `PRD(4).md` 没有这个限制。

如果严格用 `localStorage`：
- 很难支撑真实多角色访问。
- Runner API、文件上传、排行榜同步都会变得别扭。

当前建议：
- 浏览器端仅缓存会话态。
- 业务持久化放在 SQLite。
- 仍然遵守“不持久化完整 Race 数据”，只把允许公开和允许持久化的投影写入库。

### 5.3 真实认证 vs PoC 速度

PRD 要求注册/登录，但 PoC 又明确不做生产级安全。

当前建议：
- 做最小可用账号系统。
- 使用本地用户名/密码登录，密码哈希存储。
- 不引入第三方登录。

## 6. 推荐的分阶段实现顺序

### 阶段 1：项目脚手架

- 初始化 Next.js + TypeScript + Tailwind + Prisma
- 建立基础目录结构
- 建立 ROADMAP、README、环境说明

### 阶段 2：领域模型与基础设施

- 用户、赛事、队伍、报名、提交、榜单、反馈、通知、赛后披露
- 时间状态计算
- 提交频率限制
- 权限校验

### 阶段 3：Organizer 流程

- 注册/登录
- 创建赛事表单
- 修改题目/训练数据
- 反馈处理
- 赛后评论
- 一键清除比赛

### 阶段 4：Participant / Audience 流程

- 赛事列表与详情页
- 报名流程
- 提交流程
- 榜单查看
- 反馈与通知
- 公开展示页

### 阶段 5：Runner API 与任务队列

- 拉取待评测任务
- 回传评测结果
- 临时文件清理
- 榜单刷新

### 阶段 6：验收与文档

- 对照 PRD 验收单逐项自测
- 补 README、API 文档、数据边界说明
- 记录已实现与未实现项

## 7. 本轮输出

本轮仅完成：

- 需求阅读
- 设计判断
- 风险识别
- 实现计划整理

本轮未开始：

- 项目脚手架生成
- 业务代码编写
- 数据库建模
- 页面实现

## 8. 后续迭代记录

### Iteration 0

- 创建 `ROADMAP.md`
- 建立首版实现计划
- 明确先等用户确认方案，再进入编码

### Iteration 1

- 将当前工作区接入组织仓库 `SysuGroup9/ARY-for-ARY`
- 保留远端已有 `PRD.md` 和 `PoC.md` 历史
- 新增 `.gitignore`，避免提交临时目录与本地构建产物
